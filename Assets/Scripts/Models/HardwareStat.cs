namespace HackYourWay.Models
{
    /// <summary>Identifies a player hardware axis that can be upgraded via shop items.</summary>
    public enum HardwareStat { CPU = 0, Bandwidth = 1, GPU = 2 }
}
